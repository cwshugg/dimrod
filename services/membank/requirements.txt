# Python dependencies for the membank service.
#
# These are the same core dependencies used by other DImROD services.
# Install into the service's virtual environment:
#   pip install -r requirements.txt

flask
gevent
requests
PyJWT
